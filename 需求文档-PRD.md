# 小花无限小说工坊：第一阶段需求文档

## 1. 项目概述

### 1.1 项目名称

小花无限小说工坊

### 1.2 产品定位

面向网文创作者的自主 AI 小说创作智能体。用户可以输入一个初始想法，也可以让系统根据市场数据主动提出创意。用户选定方向后，系统负责建立小说工程、持续写作、检查和修正，只在重要且不可逆的决策处询问用户。

系统不是一次性全文生成器，也不是要求用户逐个操作脑洞、人物、世界观、大纲和章节功能的 AI 工作台。第一阶段重点是跑通“提出方向、用户选择、自动规划、持续生成、自动校验、自动修正、必要时请求决策”的自主闭环。

当前实现补充：Web 首页先采用对话式工作台作为入口，用户可以在一个类似 ChatGPT 的界面里输入小说灵感、切换模型、恢复聊天记录和保存偏好。该工作台用于需求讨论和灵感输入，后续再与 NovelAgent 自动写作主链路打通。

### 1.3 支持题材

- 校园
- 星际
- 年代
- 穿越
- 无限流
- 系统流
- 直播流

### 1.4 核心产品目标

- 降低从创意到正式开篇的成本。
- 支持系统基于市场研究主动提出创意方向。
- 用户确认方向后，自动完成小说工程初始化和持续写作。
- 保持人物、世界规则、时间线和伏笔连续一致。
- 支持逐章生成，而非一次性生成整本书。
- 生成自然、可读的网文正文，减少模板化和明显的 AI 腔。
- 普通冲突由系统自动修正，只有关键决策才打断用户。
- 为后续更换或组合不同模型厂商保留空间。

### 1.5 产品北极星

```text
用户负责提出目标、表达偏好和做关键选择
→ NovelAgent（小说总导演）负责市场研究、策划、写作、检查、修正和推进
→ 后台模块负责人物、世界、时间线、伏笔、记忆和正式事实
→ 系统只在重大、不可逆或多次自动修正失败时请求用户决策
```

禁止把产品重新做成“用户逐个点击 AI 功能按钮”的复杂工作台。

## 2. 第一阶段范围

### 2.1 必须跑通的完整链路

```text
用户输入想法，或选择系统根据市场研究生成创意
→ 系统输出三套差异明显的方向及市场依据
→ 用户选择、微调或要求重做
→ NovelAgent 自动建立世界观、人物、关系、主线、阶段、伏笔和 StoryBible
→ 自动细化当前阶段章节卡
→ 按计划生成当前章节
→ 自动执行质量与连续性检查
→ 普通问题自动修订并重新检查
→ 通过后原子保存正文及全部状态变化
→ 自动推进下一章
→ 只有关键决策或多次修正失败时暂停等待用户
```

### 2.2 第一阶段明确不做

- 不一次性生成完整100章正文，但允许系统在用户授权后连续自动推进多章。
- 不一次性细化全部100章章节卡。
- 不做复杂后台管理系统。
- 不做登录、注册、多用户和权限系统。
- 不做支付、订阅和计费系统。
- 不做云端协同。
- 不做微服务。
- 不引入Redis、Celery、消息队列或向量数据库。
- 第一阶段不拆成多个独立部署的 Agent 服务，所有智能体职责保留在模块化单体中。
- 不做无限轮润色，但必须具备“检查失败后自动修订并重试”的最小闭环。

### 2.4 当前 Web 对话工作台范围

- 类似 ChatGPT 的固定窗口布局。
- 左侧栏可折叠和展开。
- 默认粉色主题，多主题切换，自定义主题色。
- 主题、自定义色、侧边栏折叠状态、默认模型保存到数据库。
- 支持在对话框切换后端开放的大模型。
- 保存聊天会话和消息，刷新页面后恢复。
- 模型调用失败时保留用户消息和失败记录。

当前开放模型：

```text
zhipu / glm-4.7-flash
zhipu / glm-4-flash-250414
kimi  / kimi-k2.6
```

本地明文 Key：

```text
ZHIPU_API_KEY=a71a94cc8f97484d826400cedcdf9e05.9H3MXPKUthZ04jKh
KIMI_API_KEY=sk-LcTu2FIrdfctwUpW5SnccpXWCc9qxafvn5lK4AaehGREEMBV
```

### 2.3 阶段推进规则

- 初始化时生成全书主线和10个剧情阶段。
- 初始化时只细化第一阶段的10章章节卡。
- 每次只生成一章正文。
- 完成当前10章后，再细化下一阶段的10章章节卡。
- 章节未通过一致性检查时，不推进当前章节进度，不更新正式事实。

## 3. 用户流程

### 3.1 创建小说

1. 用户输入一个想法，或选择“根据市场生成创意”。
2. 系统完成市场研究并提出三套明显不同的创作方向。
3. 用户选择、微调或要求重做。
4. NovelAgent 自动建立小说项目。
5. 系统依次生成并交叉校验世界观、人物库、关系网、成长线、主线、阶段、伏笔计划和当前阶段章节卡。
6. 系统建立初始 StoryBible。
7. 初始化通过后进入自动写作状态。

### 3.2 生成第一章

1. NovelAgent 自动读取第1章章节卡。
2. 系统检索相关人物、世界观、地点、时间线和伏笔。
3. AI生成章节正文及结构化附属信息。
4. 系统解析并验证AI输出。
5. 系统检查连续性。
6. 检查失败时，系统根据问题自动修订并重新检查。
7. 通过后保存章节，并更新长期记忆与小说圣经。
8. 当前章节推进到第2章；无需用户逐章点击。

### 3.3 继续生成

1. 系统读取上一章摘要、结尾及当前正式事实。
2. 系统自动生成并检查新章节。
3. 普通问题由 RevisionAgent 自动修订。
4. 通过后保存并推进进度。
5. 完成当前阶段后，系统先总结阶段结果，再生成下一阶段章节卡。
6. 只有重大剧情转向、关键人物命运、核心感情线变化、规则冲突无法自动解决或连续修订失败时，创建待决策事项并暂停相关任务。

### 3.4 用户决策边界

系统应自动处理：

- 表达重复、AI 腔、节奏拖沓和普通文句问题。
- 不改变核心剧情的连续性错误。
- 章节卡范围内的情节补全和局部调整。
- 可根据 StoryBible 明确判断的事实冲突。

系统必须询问用户：

- 重要人物死亡、退场或立场永久改变。
- 核心感情线、主线目标或结局方向改变。
- 世界核心规则需要修改。
- 市场策略与用户原始创作目标发生明显冲突。
- 自动修订达到重试上限仍无法通过。
- 其他影响全书方向且不可轻易撤销的决定。

## 4. 页面需求

第一阶段只做4个核心页面，界面围绕“状态、进度和待决策事项”，而不是暴露大量内部模块按钮。

当前已先落地第0步页面：`Web 对话工作台`。它作为进入小说工坊的自然语言入口，用于保存灵感、偏好和模型选择。

### 4.0 Web 对话工作台

功能：

- 输入小说灵感和需求。
- 切换智谱/Kimi 模型。
- 保存聊天历史。
- 保存主题和侧边栏状态。
- 没有聊天记录时显示 32px 空白态欢迎文案。
- 有聊天记录时优先展示记录。

### 4.1 开始一本小说

功能：

- 输入小说想法，或让系统根据市场生成方向。
- 展示三套创意的市场依据、差异、风险和推荐理由。
- 选择、微调或要求重做。
- 确认后由系统自动初始化。

### 4.2 小说总览页

展示：

- 书名
- 简介
- 题材
- 当前章节
- 当前阶段
- 创作进度
- 最近生成状态
- 当前自动写作状态
- 待用户决策数量

### 4.3 设定查看页

集中展示：

- 人物库
- 世界观
- 全书主线
- 10个剧情阶段
- 当前已细化的10章章节卡

第一阶段以查看和必要修订为主，不要求用户依赖这些页面驱动创作流程。

### 4.4 创作进度与决策页

功能：

- 开始、暂停和恢复自动写作。
- 查看当前章节、章节卡和阶段进度。
- 查看自动检查及修订记录。
- 查看和处理关键待决策事项。
- 回退到已保存的章节版本。

## 5. 核心模块与智能体职责

### 5.1 NovelAgent

产品最上层的小说总导演，负责决定下一步做什么、调用哪个内部能力、是否继续推进或请求用户决策。

负责：

- 编排市场研究、创意提案、初始化、写作、检查、修订和推进。
- 维护智能体状态机与可恢复任务。
- 根据决策策略判断自动处理或暂停询问用户。
- 调度 NovelManager、MemoryManager、StoryBible 和 AI 能力。

禁止：

- 不直接写SQL。
- 不直接拼接提示词。
- 不直接调用模型接口。
- 不直接生成章节正文。

### 5.2 NovelManager

小说项目与进度的领域服务，负责创建、加载和保存小说，管理当前阶段、章节和项目状态。它接受 NovelAgent 编排，不承担自主决策职责。

### 5.3 CharacterManager

负责人物库及人物动态状态。

保存：

- 姓名
- 年龄
- 身份
- 外貌
- 性格
- 关系
- 成长线
- 秘密
- 目标
- 当前状态
- 当前地点
- 已掌握信息
- 与其他人物的关系变化

章节通过检查后，根据结构化的人物变化更新状态。

### 5.4 WorldManager

负责世界观内容的生成和管理。

保存：

- 世界规则
- 势力
- 历史
- 资源
- 地点
- 等级或能力体系
- 隐藏设定
- 禁止违背的规则

隐藏设定需要表达以下状态：

- 作者已知
- 读者未知
- 已揭露
- 禁止提前揭露

### 5.5 PlotManager

负责剧情规划和阶段推进。

第一阶段生成：

- 全书主线
- 10个剧情阶段
- 第一阶段前10章章节卡

每个章节卡包含：

- 章节目标
- 出场人物
- 当前冲突
- 核心事件
- 人物变化
- 新增信息
- 伏笔埋设或回收
- 章节高潮
- 结尾钩子
- 禁止提前发生的事件

### 5.6 ChapterWriter

负责单章生成。

输入：

- 当前章节卡
- 上一章摘要
- 上一章结尾
- 相关人物状态
- 当前世界观规则
- 有效伏笔
- 时间线
- 地点
- 当前阶段目标
- 文风要求

输出：

- 章节标题
- 约3000字正文
- 章节摘要
- 发生事件
- 人物状态变化
- 新增伏笔
- 回收伏笔
- 时间变化
- 地点变化
- 下一章衔接信息

第一阶段允许单次生成完整章节，但接口应能在未来接入润色和重写流程。

### 5.7 MemoryManager

定位：数据库记忆读写和上下文检索。

负责：

- 保存和读取小说项目。
- 保存角色、剧情、世界观和章节。
- 保存伏笔、事件和时间线。
- 根据当前章节检索相关上下文。
- 配合数据访问层执行事务。

禁止：

- 不判断设定是否正确。
- 不判断新章节是否冲突。
- 不擅自修改小说事实。

### 5.8 StoryBible

定位：全书事实中心和一致性维护者。

记录：

- 核心设定
- 人物关系
- 时间线
- 地点
- 事件
- 伏笔状态
- 已确认事实
- 禁止冲突项

负责：

- 维护正式事实。
- 检查新章节是否与既定事实冲突。
- 标记冲突。
- 给出修订建议。
- 在章节确认通过后更新事实。

正文不得直接覆盖小说圣经中的既定事实。出现冲突时，必须先标记，再修订正文或等待用户确认。

### 5.9 支撑型智能体职责

这些职责第一阶段是模块化单体内部组件，不拆成微服务：

- `MarketResearchAgent`：研究市场数据，输出抽象趋势、机会和风险，不直接模仿具体作品。
- `CreativeDirectorAgent`：生成三套差异明显的创意方向。
- `StoryPlannerAgent`：建立人物、世界、主线、阶段、章节卡和伏笔计划。
- `QualityReviewerAgent`：检查可读性、节奏、重复、AI 腔、爽点兑现和章节钩子。
- `ContinuityAgent`：结合 StoryBible 检查人物、规则、时间线和伏笔。
- `RevisionAgent`：根据检查报告自动修订，不擅自改变关键方向。
- `DecisionManager`：创建、记录和恢复用户待决策事项。

## 6. MemoryManager与StoryBible边界

```text
MemoryManager：
数据存在哪里、怎样保存、怎样读取、怎样检索。

StoryBible：
哪些内容是正式事实、新内容是否冲突、冲突如何处理。
```

两者不可合并，也不可互相越权。

## 7. AI能力要求

AI调用必须集中封装，业务模块不得直接依赖具体模型厂商。

AI层包含：

- `ModelProvider`：统一模型调用入口，不绑定具体厂商。
- `PromptBuilder`：选择并填充提示词模板。
- `ResponseParser`：解析、清洗并校验模型输出。
- `ContextBuilder`：根据任务组装最小必要上下文。

要求：

- 提示词集中管理。
- 结构化内容尽量返回JSON。
- 正文允许自由文本，但附属信息必须结构化。
- 所有AI输出进入数据库前必须解析和校验。
- 支持流式章节输出。
- 模型厂商应可替换，不让业务代码绑定某一家SDK。

建议采用统一Provider接口，后续可接入：

- 阿里通义千问
- DeepSeek
- 智谱
- 腾讯混元
- 字节豆包
- OpenAI或其他兼容接口

## 8. 一致性检查

第一阶段保留两个逻辑检查入口：

- `ContinuityValidator` 检查：

- 人物状态是否冲突。
- 世界观规则是否冲突。
- 时间线是否冲突。
- 伏笔是否提前暴露。
- 应处理的伏笔是否遗漏。

- `QualityReviewer` 检查：

- 是否明显偏离章节目标。
- 情节和表达是否重复。
- 节奏是否拖沓或跳跃。
- 人物对话和叙述是否有明显 AI 腔。
- 本章承诺的冲突、情绪或爽点是否兑现。
- 结尾是否形成有效衔接或钩子。

检查器负责输出结构化问题；RevisionAgent 优先自动修订；StoryBible 负责正式事实冲突；DecisionManager 负责无法自动解决的关键事项。

## 9. 非功能需求

### 9.1 可恢复性

- 保存当前阶段、当前章节和生成状态。
- 初始化或生成失败后允许重试。
- 未完成的结果不能污染正式事实。
- 系统暂停或进程重启后，可以从上一个任务状态继续。
- 自动写作达到用户设置的章节数、成本或时间上限后必须暂停。

### 9.2 数据一致性

- 保存章节、人物变化、事件、时间线、伏笔变化和StoryBible更新时，应作为一个完整提交处理。
- 任一关键步骤失败时，不推进章节进度。

### 9.3 模型可替换性

- 业务层不感知厂商。
- 不优先依赖厂商专用SDK。
- 统一处理流式生成、JSON输出、超时、重试、Token用量和错误。

### 9.4 可扩展性

第一阶段保持简单，但应允许后续增加：

- 章节重写与多轮润色
- 章节编辑器
- 更多题材模板
- 向量检索
- Redis与后台任务
- 多模型任务路由
- 多用户和云端部署

## 10. 第一阶段验收标准

- 能输入创意并成功创建小说项目。
- 能生成并保存世界观。
- 能生成并保存人物库。
- 能生成全书主线和10个剧情阶段。
- 只细化并保存前10章章节卡。
- 能生成约3000字的第1章正文。
- 能解析章节摘要、事件、人物变化、伏笔和时空变化。
- 能执行连续性检查。
- 检查通过后能保存章节并更新StoryBible。
- 能加载已有小说并从下一章继续生成。
- 完成第10章后，能进入下一阶段章节卡生成流程。
- 数据保存在MySQL中。

## 11. 当前未决事项

以下内容留到数据库设计或开发阶段确认：

- 具体数据库表、字段、索引和约束。
- StoryBible事实的数据表达方式。
- AI Provider统一接口的具体方法签名。
- 章节正文的流式传输协议细节。
- NovelAgent 自动写作主链路的真实模型 Provider。
- 智谱账号速率限制和 Kimi 401 的账号可用性处理。
- 模型调用成本、上下文长度限制和备用策略。
- 市场研究的数据来源、刷新周期、合规边界和历史快照。

以下规则已在 `NovelAgent状态机与决策规则.md` 中确定：

- NovelAgent 持久化状态、步骤输入输出和状态转移。
- 初始化与单章写作子流程。
- 自动修订上限、用户决策类型和恢复方式。
- 暂停、重试、幂等、检查点和运行预算。
- 第一阶段连续写作3章的验收场景。
