# 小花无限小说工坊：前后端启动教程

## 1. 启动前确认

本机已确认环境：

```text
JDK：17.0.19
Maven：3.9.16
MySQL：8.0.46
Node.js：24.16.0
pnpm：11.3.0
```

数据库：

```text
库名：novel_master
用户：novel_app
密码：Ww10106681@
端口：3306
```

## 2. 后端启动

进入后端目录：

```powershell
cd D:\xiaohua\novel-master-three\backend
```

本地明文模型 Key：

```powershell
$env:ZHIPU_API_KEY="a71a94cc8f97484d826400cedcdf9e05.9H3MXPKUthZ04jKh"
$env:KIMI_API_KEY="sk-LcTu2FIrdfctwUpW5SnccpXWCc9qxafvn5lK4AaehGREEMBV"
```

推荐本地启动命令：

```powershell
$env:SERVER_PORT="8080"
$env:CHAT_MODEL_PROVIDER="zhipu"
$env:CHAT_MODEL_NAME="glm-4.7-flash"
$env:ZHIPU_API_KEY="a71a94cc8f97484d826400cedcdf9e05.9H3MXPKUthZ04jKh"
$env:KIMI_API_KEY="sk-LcTu2FIrdfctwUpW5SnccpXWCc9qxafvn5lK4AaehGREEMBV"
mvn spring-boot:run
```

后端启动后访问：

```text
Swagger：http://localhost:8080/swagger-ui.html
OpenAPI：http://localhost:8080/v3/api-docs
数据库测试：http://localhost:8080/api/test/database
模型列表：http://localhost:8080/api/chat/models
偏好设置：http://localhost:8080/api/chat/preferences
```

当前本地开发默认：

```text
APP_AUTH_ENABLED=false
TEST_ENDPOINTS_ENABLED=true
SWAGGER_ENABLED=true
MODEL_PROVIDER=fake
CHAT_MODEL_PROVIDER=zhipu
CHAT_MODEL_NAME=glm-4.7-flash
```

说明：

- `MODEL_PROVIDER=fake` 控制 NovelAgent 自动写作主链路。
- `CHAT_MODEL_PROVIDER=zhipu` 控制 Web 对话工作台默认模型。
- Web 对话工作台当前支持智谱和 Kimi。

## 3. 前端启动

进入 Web 目录：

```powershell
cd D:\xiaohua\novel-master-three\web
```

安装依赖：

```powershell
pnpm install
```

启动 Web：

```powershell
pnpm dev
```

访问：

```text
http://localhost:3000
```

Web 默认通过 `next.config.ts` 把请求转发到后端：

```text
/api/* → http://localhost:8080/api/*
```

页面中的接口请求统一使用：

```text
/api/{控制器}/{接口名}
```

当前 Web 已接入：

```text
GET  /api/chat/models
GET  /api/chat/preferences
PUT  /api/chat/preferences
GET  /api/chat/conversations
GET  /api/chat/conversations/{conversationId}
POST /api/chat/messages
```

## 4. 常用检查命令

后端测试：

```powershell
cd D:\xiaohua\novel-master-three\backend
mvn test
```

Web 检查：

```powershell
cd D:\xiaohua\novel-master-three\web
pnpm lint
pnpm typecheck
pnpm build
```

检查端口：

```powershell
Get-NetTCPConnection -LocalPort 8080 -ErrorAction SilentlyContinue
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue
```

## 5. 当前模型账号状态

最近一次联调结果：

```text
GET /api/chat/models：正常
PUT /api/chat/preferences：正常
POST /api/chat/messages：已打到真实模型供应商
智谱：返回账户速率限制
Kimi：返回 401 UNAUTHORIZED
```

后端已经保存用户消息、聊天会话和模型失败记录。恢复可用模型账号后，继续调用同一个 `/api/chat/messages` 接口即可。
