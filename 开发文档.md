# 小花无限小说工坊：总开发文档

## 1. 当前架构

```text
Web：Next.js + TypeScript + Tailwind CSS
Backend：Java 17 + Spring Boot + Maven
Database：MySQL 8.0 + Flyway
Admin：Vue 3 + TypeScript + Vite + Element Plus（尚未初始化代码）
App：Flutter + Riverpod（尚未初始化代码）
```

当前已开发重点：

```text
Web 对话工作台
→ Spring Boot /api/chat/**
→ MySQL V3 聊天工作台表
→ 智谱/Kimi 模型接口
```

NovelAgent 自动写作主链路当前仍使用 Fake Provider。

## 2. 目录

```text
novel-master-three/
├── backend/   Spring Boot 后端
├── web/       Next.js 用户 Web
├── admin/     Vue 3 管理系统文档
├── app/       Flutter App 文档
├── 需求文档-PRD.md
├── 技术栈分析.md
├── 开发接力文档.md
└── 前后端启动教程.md
```

## 3. 当前接口规则

用户端业务 API：

```text
/api/{控制器}/{接口名}
```

管理端业务 API：

```text
/admin-api/{控制器}/{接口名}
```

Swagger：

```text
http://localhost:8080/swagger-ui.html
http://localhost:8080/v3/api-docs
```

## 4. 已完成接口

测试接口：

```text
GET /api/test/ping
GET /api/test/database
```

NovelAgent 原型接口：

```text
POST /api/novel-agent/runs
GET  /api/novel-agent/runs/{runId}
POST /api/novel-agent/runs/{runId}/creative-selection
GET  /api/novel-agent/runs/{runId}/chapters
GET  /api/novel-agent/runs/{runId}/decisions
POST /api/novel-agent/runs/{runId}/decisions/{decisionId}/resolve
POST /api/novel-agent/runs/{runId}/resume
```

Web 对话工作台接口：

```text
GET  /api/chat/models
GET  /api/chat/preferences
PUT  /api/chat/preferences
GET  /api/chat/conversations
POST /api/chat/conversations
GET  /api/chat/conversations/{conversationId}
POST /api/chat/messages
```

## 5. 数据库

当前 Flyway：

```text
V1__initialize_schema.sql
V2__create_novel_agent_core.sql
V3__create_chat_workspace.sql
```

V3 聊天工作台表：

```text
app_user_profile
user_ui_preference
chat_conversation
chat_message
chat_model_call_record
```

## 6. 明文 Key

本地明文配置：

```text
DB_PASSWORD=Ww10106681@
ZHIPU_API_KEY=a71a94cc8f97484d826400cedcdf9e05.9H3MXPKUthZ04jKh
KIMI_API_KEY=sk-LcTu2FIrdfctwUpW5SnccpXWCc9qxafvn5lK4AaehGREEMBV
```

后端 `.env.example` 已写入当前本地明文值。

## 7. 启动

完整启动步骤见：

```text
前后端启动教程.md
```

常用命令：

```powershell
cd D:\xiaohua\novel-master-three\backend
mvn spring-boot:run
```

```powershell
cd D:\xiaohua\novel-master-three\web
pnpm dev
```

## 8. 验证

后端：

```powershell
cd D:\xiaohua\novel-master-three\backend
mvn test
```

前端：

```powershell
cd D:\xiaohua\novel-master-three\web
pnpm lint
pnpm typecheck
pnpm build
```

最近验证状态：

```text
后端 mvn test：9 tests passed
Web pnpm lint：通过
Web pnpm typecheck：通过
Web pnpm build：通过
/api/chat/preferences：通过
/api/chat/models：通过
智谱真实调用：账户速率限制
Kimi 真实调用：401 UNAUTHORIZED
```

## 9. 下一步

```text
恢复可用模型账号
→ 复测 /api/chat/messages 成功回复
→ 将 Web 对话工作台与 NovelAgent 创建小说流程打通
→ 接入 NovelAgent 真实 ModelProvider
→ 验证连续10章质量、一致性、耗时和用量
```
