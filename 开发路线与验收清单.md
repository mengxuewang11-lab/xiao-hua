# 小花无限小说工坊：开发路线与验收清单

## 1. 执行规则

本文档用于安排开发顺序和后期验收。

1. 前一步未通过验收，不进入下一步。
2. 每一步必须留下可检查的文档、代码、接口或测试结果。
3. 核心自主闭环完成前，不优先开发 App、复杂后台、支付和多用户。
4. 不能用“页面做完了”或“接口有返回”代替业务验收。
5. 新功能如果没有提升自主规划、持续写作、长篇一致性或自动修正能力，应降低优先级。

## 2. 第一阶段最终目标

```text
输入一个想法，或选择市场生成创意
→ 系统提出三套差异明显的方向
→ 用户选择其中一套
→ 系统自动建立人物、世界、大纲、伏笔和 StoryBible
→ 系统自动连续写作3章
→ 每章自动执行质量与连续性检查
→ 普通问题自动修订
→ 关键问题暂停并请求用户决策
→ 服务重启后可以继续任务
```

完成以上链路，才算核心原型成立。

## 3. 十二步开发路线

### 第1步：冻结产品边界

目标：明确 Three 是自主小说智能体，不是 AI 功能按钮集合。

交付物：

- `需求文档-PRD.md`
- `开发接力文档.md`
- 本验收清单

验收：

- 用户只负责目标、偏好和关键选择。
- NovelAgent 负责研究、规划、写作、检查、修正和推进。
- 第一阶段明确不做复杂后台、支付、多用户和微服务。
- 文档主流程不存在互相冲突。

状态：已完成。

### 第2步：设计 NovelAgent 状态机

目标：将自主创作定义成可执行、可恢复、可测试的状态机。

交付物：

- `NovelAgent状态机与决策规则.md`
- 状态转换、进入条件、输出、失败处理和恢复规则
- 章节数、成本、时间和修订次数预算

验收：

- 任一任务都能判断当前状态和合法下一步。
- 服务重启后可以恢复。
- 重复请求不会重复创建小说或章节。
- 每个失败状态都有重试、暂停或终止规则。
- 不使用零散布尔值代替明确状态。

状态：已完成并实现。状态转换、预算暂停、用户决策和启动恢复均已实测。

### 第3步：确定用户决策规则

目标：明确系统什么时候自动处理，什么时候必须询问用户。

交付物：

- 决策类型、严重程度、选项和恢复动作
- 自动修订上限及升级条件

验收：

- 文句、节奏、普通重复等问题不打扰用户。
- 重要人物死亡、核心关系、主线、结局和世界规则改变必须询问。
- 决策事项包含原因、影响、系统建议和选项。
- 用户处理后原任务可以恢复。
- 同一问题不会重复创建多个决策事项。

状态：已完成并实现。重要人物命运场景已验证暂停、决策和恢复。

### 第4步：设计数据库与事务

目标：根据状态机设计 MySQL，而不是围绕页面堆表。

必须覆盖：

- 小说项目和创作偏好
- Agent 运行、任务、步骤、重试和预算
- 市场快照、趋势报告和创意候选
- 人物、关系、世界规则、阶段和章节卡
- 正文、摘要、章节版本和修订记录
- 事件、时间线、地点和伏笔生命周期
- StoryBible 正式事实和冲突记录
- 质量检查、连续性检查和自动修订
- 用户待决策事项
- 模型调用、Token、费用和错误

交付物：

- ER 图
- 表、字段、枚举、索引和约束说明
- 事务边界和幂等策略
- 数据版本、保留和删除策略

验收：

- 能完整保存和恢复 NovelAgent 状态。
- 章节及人物、事件、时间线、伏笔和 StoryBible 更新可以原子提交。
- 未通过检查的草稿不会污染正式事实。
- 同一任务重复执行不会产生重复章节。
- 不使用 MySQL `root`，不使用 Hibernate 自动改表。

状态：第一阶段已完成。`数据库设计-第一阶段.md` 和 Flyway V2 已落地；临时库与正式库迁移均通过。

### 第5步：建立业务数据库和后端骨架

目标：用 Flyway 创建正式业务表和模块边界。

交付物：

- 新增 Flyway 迁移
- Entity、DTO、Repository、统一错误响应
- `agent`、`novel`、`memory`、`storybible`、`decision`、`quality`、`ai` 模块
- OpenAPI 中文契约

验收：

- 空数据库可从 V1 顺序迁移到最新版。
- Flyway 与 JPA 校验通过。
- Repository 集成测试通过。
- 已执行的迁移文件没有被修改。
- Swagger 包含中文用途、参数、响应和权限说明。

状态：第一阶段已完成。当前使用 Spring JDBC，不使用 Hibernate 自动改表；V2 创建12张表和19个外键。

### 第6步：实现 Fake ModelProvider

目标：不依赖真实模型，先证明业务流程和恢复机制正确。

交付物：

- 统一 `ModelProvider`
- `FakeModelProvider`
- Prompt、上下文、响应解析和结构校验接口
- 成功、超时、无效格式和冲突测试场景

验收：

- 更换 Provider 不影响领域模块。
- Fake Provider 能返回创意、规划、章节和检查结果。
- 能模拟超时、限流、无效 JSON 和内容冲突。
- AI 原始输出不会未经解析直接入库。

状态：核心完成。已支持创意、初始化、章节、质量检查、连续性冲突和自动修订；超时、限流和无效JSON故障注入仍待补充。

### 第7步：跑通创意与初始化链路

目标：从一句想法开始，完成三套创意和小说自动初始化。

交付物：

- 想法输入、创意候选、选择和微调接口
- 世界观、人物、关系、主线、阶段、伏笔和前10章章节卡
- 初始 StoryBible

验收：

- 三套创意在核心冲突、人物关系和发展方向上明显不同。
- 每套包含目标读者、市场依据、差异点和风险。
- 用户只需选择一次即可开始初始化。
- 初始化失败不会把半成品标记为完成。
- 初始化结果之间不存在明显设定冲突。

说明：第一版可使用固定市场样本，不让网页抓取阻塞核心闭环。

状态：已完成。真实运行会生成三套方向，选择后建立小说基础版本和前10章章节卡。

### 第8步：跑通单章生成闭环

目标：自动完成一章的生成、检查、修订、保存和状态更新。

交付物：

- ContextBuilder、ChapterWriter
- ContinuityValidator、QualityReviewer
- RevisionAgent
- 章节确认事务

验收：

- 根据章节卡和最小上下文生成正文及结构化元数据。
- 能检查人物、世界规则、时间线、伏笔和章节目标。
- 能识别明显重复、节奏问题、AI 腔和无效结尾钩子。
- 普通问题自动修订并重新检查。
- 只有通过检查的章节才能更新 StoryBible。
- 达到修订上限后创建待决策事项。

状态：核心完成。每章都经历生成、结构校验、质量检查、自动修订、连续性检查和事务提交。

### 第9步：实现连续3章自主写作

目标：NovelAgent 无需用户逐章点击，自动连续完成3章。

交付物：

- 自动运行控制器
- 开始、暂停、恢复和停止接口
- 章节、费用、时间和失败次数预算
- 任务恢复与幂等控制

验收：

- 选择创意后可自动初始化并连续写3章。
- 三章人物状态、时间线、地点和伏笔无明显冲突。
- 每章都有检查与修订记录。
- 达到预算后安全暂停。
- 第2章中途重启后可以恢复，不重复第1章。
- 关键决策暂停后，用户处理即可继续。

这是第一阶段最重要的里程碑。

状态：核心里程碑已通过。已实测正常三章、关键决策暂停恢复、预算暂停恢复和服务重启恢复；手动暂停/停止与多实例执行租约仍待补充。

### 第10步：接入真实模型并扩展到10章

目标：验证真实模型下的稳定性、质量、成本和一致性。

交付物：

- 至少一个真实 ModelProvider
- 超时、限流、重试和备用模型策略
- 用量与成本记录
- 连续10章测试报告

验收：

- 连续10章任务可完成或可恢复。
- 单次限流或超时不会丢失状态。
- 关键人物、核心设定和时间线无明显矛盾。
- 伏笔状态可追踪。
- 每章质量检查有结构化结果。
- 成本、耗时和失败次数可查看。
- 人工抽查达到“可继续修改”的最低质量。

### 第11步：接入真实市场研究

目标：根据合法获取的市场数据提出原创方向。

交付物：

- 数据源清单和合规边界
- 刷新、缓存、历史快照和失败降级
- 趋势抽象报告
- 原创性和同质化风险检查

验收：

- 原始作品数据只进入市场分析，不直接进入正文生成。
- 输出抽象趋势、机会、拥挤度和风险。
- 数据源失效不影响已有小说继续写作。
- 不复用具体书名、人物、专有设定或标志性事件链。
- 能比较不同日期的趋势，而不是只抓一次榜单。

### 第12步：开发精简 Web

目标：为核心智能体提供简单、明确的界面。

只做：

- 开始一本小说
- 查看并选择三套创意
- 查看自动写作进度
- 阅读章节和设定
- 处理待决策事项
- 暂停、恢复和停止

验收：

- 用户无需逐项点击生成人物、世界、大纲和章节。
- 首页能看清系统正在做什么、做到哪一步、为何暂停。
- 决策处理后能恢复任务。
- 页面刷新不丢运行状态。
- 普通用户无需技术说明即可完成核心流程。

## 4. 核心完成后的扩展顺序

1. 多小说项目和章节版本管理。
2. 用户修改偏好学习。
3. 连续30章稳定性和质量提升。
4. 登录、多用户和权限。
5. 部署、监控、备份和成本控制。
6. 管理后台。
7. Flutter App。
8. 会员、支付和商业化。
9. 短剧、漫画、有声书等 IP 扩展。

## 5. 总体验收红线

出现以下任一情况，不能宣称核心原型完成：

- 仍需要用户逐个点击生成人物、世界、大纲和每一章。
- 只能生成正文，不能维护人物、时间线、伏笔和正式事实。
- 检查失败后只提示用户，不会自动修订。
- 服务重启后任务无法恢复。
- 重试会产生重复章节或重复数据。
- 关键冲突发生后仍自动推进并污染 StoryBible。
- 三套创意只是更换名称，核心故事骨架相同。
- 没有运行预算，可能无限调用模型。
- 页面很多，但没有跑通连续3章自主闭环。

## 6. 时间参考

以一名开发者配合 AI 编程、需求保持稳定为前提：

| 范围 | 预计时间 |
|---|---:|
| 第1至3步：产品、状态机、决策规则 | 1至2周 |
| 第4至6步：数据库、后端、Fake Provider | 2至4周 |
| 第7至9步：核心闭环与连续3章 | 3至6周 |
| 第10步：真实模型与连续10章 | 2至4周 |
| 第11至12步：市场研究与精简 Web | 3至6周 |

核心原型预计约 6 至 12 周；包含市场研究、真实模型优化和精简 Web 的可用内测版预计约 3 至 5 个月。时间以验收结果为准，不以代码量和页面数量为准。

## 7. 当前执行顺序

2026-06-16 本地复检状态：

- 后端可在 `8080` 端口运行。
- MySQL 8.0.46 连接正常，Flyway 当前版本为 2。
- Swagger UI 可访问。
- `localhost:3000` CORS 预检通过。
- 本地模式默认关闭认证，接口联调无需 Token；认证骨架保留，后续可通过 `APP_AUTH_ENABLED=true` 开启。
- Node.js、npm、pnpm 已可用；Flutter/Android 环境尚未安装。
- Web 用户网站已初始化 Next.js 骨架，`localhost:3000` 页面和 `/api/test/database` rewrite 验证通过；当前还不是第12步完整业务 Web。

```text
补充模型失败、无效结构、事务回滚和并发幂等测试
→ 第10步：接入首个真实 ModelProvider
→ 连续10章质量、成本和一致性验证
→ 第11步：真实市场研究
→ 第12步：精简 Web
```

当前 Fake Provider 只证明自主流程和恢复机制成立，不能用于评价真实小说质量。
