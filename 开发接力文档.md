# 小花无限小说工坊：开发接力文档

## 1. 接手摘要

这是一个自主 AI 网文创作智能体平台。用户输入想法，或让系统根据市场数据提出创意；用户选定方向后，系统主动完成小说规划、持续写作、检查和修正，只在重要决策处询问用户。

长期包含三个客户端：

- Flutter App。
- Next.js 用户网站。
- Vue 3 管理系统。

三个客户端共用一个 Java 17 + Spring Boot + Maven 后端和一个 MySQL 数据库。后端已经跑通基于 Fake Provider 的第一阶段自主创作原型；Web 用户网站已完成 Next.js 启动骨架、年轻化 ChatGPT 风格工作台、侧边栏折叠/展开、主题与模型偏好持久化、聊天会话和消息落库。Web 对话工作台已接入智谱/Kimi HTTP 模型接口；NovelAgent 自动写作主链路仍使用 Fake Provider。

### 产品北极星

```text
用户负责目标、偏好和关键选择
→ NovelAgent（小说总导演）负责研究、规划、写作、检查、修正和推进
→ 普通问题自动处理
→ 重大、不可逆或多次修正失败的问题才请求用户决策
```

不要把 Three 做成架构更正规的 Two。Two 的主要问题是功能入口复杂、依赖用户逐项点击、缺少真正自主推进和可靠长篇记忆。Three 必须以“目标驱动的小说智能体”为中心，人物库、世界观库、大纲、时间线和伏笔是后台能力，不是要求用户逐个操作的主流程。

## 2. 当前正式决策

```text
App：Flutter + Riverpod + Material 3
Web：Next.js + TypeScript + Tailwind CSS + shadcn/ui
Admin：Vue 3 + TypeScript + Vite + Element Plus
Backend：Java 17 + Spring Boot + Maven
Database：MySQL 8.0 + Flyway
API：REST + SSE + OpenAPI
AI：统一 ModelProvider，厂商未定
```

业务 API 路径统一采用 `/api/{控制器}/{接口名}`，管理系统后续采用 `/admin-api/{控制器}/{接口名}`。前端可通过 `baseURL=http://localhost:8080/api` 省略重复前缀，但真实请求地址仍然带 `/api`。Swagger 是文档入口，仍为 `/swagger-ui.html` 与 `/v3/api-docs`。

Flyway 是数据库结构管理的唯一方式，不是可选工具。已执行迁移不可回头修改，后续变化必须新增版本前滚。

历史方案 Python/FastAPI/SQLite、React Native/Expo、React/Ant Design 管理端已经废弃。

## 3. 当前进度

已完成：

- 产品北极星、自主创作闭环和用户决策原则定义。
- NovelAgent 持久化状态、步骤输入输出、转移条件和初始化/单章子流程设计。
- 自动修订与用户决策边界、重试上限、恢复、幂等和运行预算规则。
- 第一阶段连续写作3章的端到端验收场景。
- NovelManager、MemoryManager、StoryBible 等领域边界定义。
- 三端和共享后端技术选型。
- Java、Maven、Git、MySQL、Node.js、pnpm 环境配置。
- App、Web、Admin 规划目录。
- 创建 `novel_master` 数据库和 `novel_app` 非 root 本地账户。
- 创建 Spring Boot 3.5.15 Maven 后端骨架。
- 创建并执行 Flyway `V1__initialize_schema.sql`。
- 设计第一阶段数据库并执行 `V2__create_novel_agent_core.sql`，创建12张核心表。
- 执行 `V3__create_chat_workspace.sql`，创建 Web 对话工作台表：本地用户、UI 偏好、聊天会话、聊天消息和模型调用记录。
- 实现 NovelAgent 状态机、步骤审计、预算、检查点和启动恢复。
- 实现统一 `NovelModelProvider` 与确定性 Fake Provider。
- 实现输入想法、三套创意、用户选择、自动初始化和前10章章节卡。
- 跑通自动连续写作3章，每章约2961字。
- 跑通质量问题自动修订，未修订草稿不会进入正式章节。
- 跑通重要人物命运决策暂停，用户处理后恢复并完成3章。
- 跑通预算暂停、提高预算后恢复。
- 跑通模拟章节中断后的服务重启恢复，没有重复章节。
- 提供7个带中文 Swagger 说明的 NovelAgent API。
- 提供 `/api/test/ping` 与 `/api/test/database` 临时测试接口。
- 验证 Next.js `localhost:3000` 与 Vite `localhost:5173` CORS。
- 集成 springdoc OpenAPI/Swagger UI，接口包含中文用途和响应说明。
- 接入 Spring Security 无状态认证骨架；当前本地单机模式默认关闭认证，开启 `APP_AUTH_ENABLED=true` 后受保护接口使用 Bearer Token。
- 为后续 JWT 登录认证预留统一的 `Authorization: Bearer <token>` 格式。
- 2026-06-16 本地复检通过：后端 `8080` 端口运行，MySQL/Flyway 正常，Swagger 可访问，CORS 预检通过，NovelAgent 创建运行接口可无 Token 调通。
- Web 对话工作台新增 `/api/chat/**`：模型列表、偏好保存、会话列表、会话详情和发送消息接口，Swagger 含中文说明和 Bearer 认证标记。
- Web 首页已通过真实后端读取偏好、模型和聊天记录；主题、自定义色、侧边栏折叠状态、默认模型、会话和消息会保存到 MySQL。
- 智谱默认模型为 `glm-4.7-flash`；Kimi 模型为 `kimi-k2.6`。当前联调时智谱账号返回速率限制，Kimi key 返回 `401 UNAUTHORIZED`，后端已验证错误映射和失败记录。

未完成：

- 真实市场数据采集与合规策略；当前为 Fake 研究结果。
- NovelAgent 首个真实 ModelProvider、超时限流、备用模型和连续10章质量验证。
- 更完整的故障注入测试，包括模型超时、无效JSON和数据库事务失败。
- 人物、关系、时间线和伏笔的细粒度结构化表；第一阶段基础数据主要保存在版本化JSON和 StoryBible 事实中。
- 手动暂停/停止、执行租约和多实例并发控制。
- 用户登录、JWT Access/Refresh Token、角色权限和管理操作审计。
- 管理系统和 App 框架初始化。
- Web 业务页面仍需继续扩展到小说项目、章节、待决策和导出；当前完成对话工作台、主题/侧边栏/模型偏好持久化、模型切换、聊天记录落库和 `/api/*` 代理验证。
- Flutter/Android 本地环境。

## 4. 本机开发环境

```text
JDK：17.0.19
JAVA_HOME：D:\Java\jdk-17

Maven：3.9.16
MAVEN_HOME：D:\Java\apache-maven-3.9.16
本地仓库：D:\Java\maven-repository

Git：2.54.0
路径：D:\Git

MySQL Server：8.0.46
服务：MySQL80，自动启动
端口：3306
数据目录：C:\ProgramData\MySQL\MySQL Server 8.0\Data
Workbench：8.0.47

NVM：1.2.2
NVM_HOME：D:\nvm
Node 入口：D:\NodeJS
Node.js：24.16.0 LTS
npm：11.13.0
pnpm：11.3.0
pnpm Store：D:\pnpm-store
Corepack：0.35.0
```

Flutter、Dart、Android Studio、Android SDK、ADB 和模拟器尚未安装。

## 5. 目标目录

```text
novel-master-three/
├── app/                 # Flutter
├── web/                 # Next.js
├── admin/               # Vue 3
├── backend/             # Spring Boot
├── README.md
├── 需求文档-PRD.md
├── 技术栈分析.md
└── 开发接力文档.md
```

目前各业务目录只应包含文档。数据库设计确认后再生成框架。

例外：`backend` 已包含最小可运行 Spring Boot 骨架和 Flyway 连接探针，三个客户端目录仍只有文档。

## 6. 后端架构

采用模块化单体，而不是微服务：

```text
Controller
→ Application Service / Manager
→ Domain
→ Repository
→ MySQL
```

建议模块：

```text
identity
agent
market
novel
character
world
plot
chapter
memory
storybible
quality
decision
ai
admin
shared
```

管理接口使用 `/admin-api`，用户端接口使用 `/api`。两者共用领域服务，但权限、DTO 和审计要求分开。

## 7. 核心业务边界

### NovelAgent

产品最上层的小说总导演。负责编排市场研究、创意提案、小说初始化、逐章写作、质量检查、连续性检查、自动修订和任务推进；根据决策策略判断继续执行还是暂停询问用户。

NovelAgent 是应用层编排器，不直接写 SQL、不直接拼提示词、不直接调用厂商 SDK，也不把未经校验的 AI 输出写入正式事实。

### NovelManager

负责小说项目、阶段、章节和生成状态的领域管理，接受 NovelAgent 调度。它不承担“下一步做什么”的自主决策。

### MemoryManager

负责数据读写与上下文检索，不判断内容是否与正式事实冲突。

### StoryBible

负责正式事实、一致性判断、冲突记录和修订建议，不负责底层数据访问实现。

### AI 模块

负责 Provider、上下文、提示词、响应解析、重试和用量。Web 对话工作台已接入智谱/Kimi；NovelAgent 自动写作主链路在真实 Provider 稳定前继续使用 Fake Provider。

### DecisionManager

负责关键待决策事项的创建、分类、优先级、用户选择和任务恢复。普通文句、局部节奏和可明确修复的一致性问题不得打扰用户。

必须询问用户的典型事项：

- 重要人物死亡、永久退场或立场根本变化。
- 核心感情线、主线目标、结局方向改变。
- 世界核心规则需要修改。
- 市场建议与用户原始目标明显冲突。
- 自动修订达到上限仍不能通过检查。

## 8. 初始化主链路

```text
接收用户想法，或执行市场研究
→ 生成三套差异明显的创意方向
→ 等待用户选择、微调或重做
→ 创建小说草稿
→ 自动生成并校验世界观、人物、关系、主线、阶段和伏笔计划
→ 只生成第一阶段10章章节卡
→ 建立初始 StoryBible
→ 一个事务保存已校验结果
→ 进入自动写作状态
```

任一步失败时保留可恢复任务状态，不得把半成品标记为初始化完成。

## 9. 单章主链路

```text
读取当前章节卡
→ 检索相关人物、规则、时间线和伏笔
→ 构建最小上下文
→ ModelProvider 生成正文和结构化元数据
→ 解析与字段校验
→ 质量检查与连续性检查
→ 普通问题自动修订并重新检查
→ StoryBible 判定
→ 一个事务保存章节及全部状态变化
→ 自动推进当前章节并开始下一任务
```

出现冲突时：

```text
记录冲突
→ 不更新正式事实
→ 不推进章节
→ 优先自动修订
→ 只有关键冲突或达到修订上限时等待用户确认
```

自动连续写作必须受章节数、模型成本、时间和失败次数预算约束，达到任一上限后安全暂停。

## 10. 数据库设计范围

首轮设计必须覆盖：

- 小说项目与进度。
- 初始化和章节生成任务。
- 人物档案、动态状态和关系。
- 世界规则、势力、地点、资源和隐藏设定。
- 全书主线、剧情阶段和章节卡。
- 章节正文、摘要和版本。
- 事件、时间线和地点变化。
- 伏笔及其生命周期。
- StoryBible 正式事实。
- 冲突记录及处理状态。
- 模型调用记录和用量。
- NovelAgent 运行、步骤和恢复状态。
- 市场研究快照、趋势结论和创意候选。
- 质量检查、自动修订尝试和结果。
- 用户待决策事项及其处理记录。

首轮暂不加入：

- 支付和订单。
- 向量表。
- Elasticsearch。
- 微服务拆分表。
- 复杂多租户。

## 11. 推荐开发顺序

1. 以 `NovelAgent状态机与决策规则.md` 为依据，输出覆盖自主任务、小说事实、质量检查和待决策事项的 MySQL ER 设计。
2. 评审状态、事务、幂等、检查点和草稿/正式事实隔离是否能被表结构完整表达。
3. 以新增 Flyway 版本迁移创建正式业务表。
4. 定义实体、DTO、Repository 和统一错误响应。
5. 实现 MemoryManager、StoryBible 和任务恢复边界。
6. 实现 Fake Model Provider。
7. 跑通最小自主闭环：输入想法 → 三套创意 → 用户选择 → 自动初始化 → 自动写3章 → 自动检查与修订。
8. 接入真实 ModelProvider，将稳定性目标扩展到连续10章。
9. 初始化精简 Next.js 用户 Web，只提供开始小说、进度、阅读和待决策入口。
10. 核心闭环稳定后再初始化管理系统和 Flutter App。

## 12. 当前测试接口与本地运行状态

当前本机按“只在本地跑”的方式配置，默认端口为 `8080`，默认使用 Fake Provider，默认关闭 API Token 认证，默认开启测试接口。启动命令：

```powershell
cd D:\xiaohua\novel-master-three\backend
mvn spring-boot:run
```

测试接口：

```text
GET http://localhost:8080/api/test/ping
GET http://localhost:8080/api/test/database
```

Swagger：

```text
http://localhost:8080/swagger-ui.html
http://localhost:8080/v3/api-docs
```

当前本地模式下不需要 `APP_AUTH_TOKEN`。如果要测试认证骨架，再设置：

```text
APP_AUTH_ENABLED=true
APP_AUTH_TOKEN=<本地长随机令牌>
```

Swagger 点击 `Authorize` 后只输入令牌值，不输入 `Bearer ` 前缀。

当前静态令牌只用于本地开发。正式阶段替换为登录签发的 JWT Access/Refresh Token，并增加用户、角色、权限和审计能力。

最近一次复检结果：

- 数据库 `novel_master`。
- 应用账户 `novel_app@127.0.0.1`。
- MySQL 8.0.46。
- `utf8mb4` 与 `utf8mb4_0900_ai_ci`。
- Flyway 版本 3。
- V1 迁移探针、V2 NovelAgent 核心表、V3 Web 对话工作台表。
- Swagger 返回 200。
- `localhost:3000` CORS 预检返回 200。
- `mvn test` 通过，9 tests passed。

NovelAgent Fake 原型接口：

```text
POST /api/novel-agent/runs
GET  /api/novel-agent/runs/{runId}
POST /api/novel-agent/runs/{runId}/creative-selection
GET  /api/novel-agent/runs/{runId}/chapters
GET  /api/novel-agent/runs/{runId}/decisions
POST /api/novel-agent/runs/{runId}/decisions/{decisionId}/resolve
POST /api/novel-agent/runs/{runId}/resume
```

这些接口遵循 `/api/{控制器}/{接口名}`：其中 `novel-agent` 是控制器路径，`runs`、`runs/{runId}/chapters` 等是接口路径。

当前仓库为了本地单机开发保留了本机默认数据库连接配置。任何公开部署、共享环境或生产环境都必须改回环境变量注入，关闭测试接口，并开启正式认证。

## 13. 测试主线

- 初始化失败不会污染正式数据。
- 幂等重试不会重复创建小说、任务或章节。
- 冲突章节不会更新 StoryBible 或推进进度。
- 合法章节能原子保存人物、事件、时间线和伏笔变化。
- 普通质量或连续性问题能自动修订，不创建无意义的用户决策。
- 关键且不可逆的问题会暂停任务并创建可恢复的待决策事项。
- 完成第10章后生成下一阶段章节卡。
- 更换 Fake/真实 Provider 不影响领域模块。
- 用户 API 无法访问管理接口。
- 管理操作产生审计记录。
- Swagger 中每个业务接口都有中文用途、参数、响应和权限说明。

## 14. 接力约束

不要擅自：

- 将产品重新设计成需要用户逐项点击脑洞、人物、世界观、大纲和章节按钮的工作台。
- 在 NovelAgent 自主闭环跑通前优先开发复杂 App、管理后台、支付、多租户或精细权限。
- 将后端拆成微服务。
- 让客户端直接调用模型厂商。
- 让客户端直接绑定 OpenAI 或任一模型厂商。
- 让 AI 原始输出跳过校验直接入库。
- 合并 MemoryManager 与 StoryBible。
- 使用 Hibernate 自动更新生产表结构。
- 修改或删除已执行的 Flyway 迁移文件。
- 在生产环境执行 Flyway `clean`。
- 将 Flyway `repair` 加入自动发布流程。
- 使用 MySQL `root` 作为应用账户。
- 将开发静态 Token 用于生产。
- 只在 Swagger 标锁而不在 Spring Security 中执行真实权限校验。
- 在数据库设计未确认前批量生成业务代码。

当前最重要的下一项成果是：

1. **补足模型失败、无效结构、事务回滚和并发幂等的自动化集成测试。**
2. **处理真实模型账号可用性：智谱速率限制、Kimi 401，需要更换/恢复可用 key 后复测成功回复。**
3. **将真实 ModelProvider 接入 NovelAgent，并验证连续10章的质量、一致性、耗时和费用。**

完整开发阶段、交付物和验收标准见 `开发路线与验收清单.md`。后续必须逐项核对，不能用“页面完成”或“接口有返回”代替阶段验收。

### 下次接手必读

开始任何业务开发前，先确认实现是否服务于以下验收目标：

```text
输入一个想法或选择市场创意
→ 系统提出三套方向
→ 用户只做一次关键选择
→ 系统自动建立小说工程
→ 自动连续写作3章
→ 每章自动检查和修订
→ 普通问题不打扰用户
→ 关键问题生成待决策事项并可在用户处理后恢复
```

如果一项功能只是增加操作入口，却没有提高自主规划、长篇一致性、自动修正或持续推进能力，应降低优先级。

状态机、决策和恢复规则以项目根目录的 `NovelAgent状态机与决策规则.md` 为唯一设计基线；数据库与代码实现发现冲突时，应先修改并评审该文档。
