# 小花无限小说工坊：技术栈总览

## 1. 文档状态

本文记录截至 2026-06-16 已确认的正式技术方向。此前的 Python、FastAPI、SQLite、React Native 和 React 管理端方案均已废弃，不再作为实现依据。

## 2. 总体架构

```text
App：Flutter
Web：Next.js
Admin：Vue 3
       ↓ REST / SSE
共享后端：Java 17 + Spring Boot + Maven
       ↓
MySQL 8.0
       ↓
统一 ModelProvider / ChatModelGateway → 智谱、Kimi、后续可扩展模型厂商
```

后端先采用模块化单体：

- 一个部署单元。
- 一个 MySQL 数据库。
- 用户 API 与管理 API 分区。
- 领域模块保持边界。
- 真实需要出现前不拆微服务、不引入消息队列。

## 3. 三端选型

| 平台 | 核心技术栈 | UI 与设计 |
|---|---|---|
| App | Flutter、Dart、Riverpod、go_router、Dio | Material 3、Cupertino、自定义设计系统 |
| 用户 Web | Next.js、React、TypeScript | Tailwind CSS、shadcn/ui |
| 管理系统 | Vue 3、TypeScript、Vite | Element Plus、SCSS、CSS Variables |

### 3.1 App 配套

```text
数据模型：Freezed + json_serializable
安全存储：flutter_secure_storage
本地数据：Drift（需要离线能力时引入）
组件预览：Widgetbook
动效：Rive / Lottie（按设计需求引入）
测试：flutter_test + Golden Test + integration_test
```

App 重点是高质量阅读体验、移动创作、生成状态和跨平台视觉一致性。复杂富文本编辑优先放在 Web。

### 3.2 用户 Web 配套

```text
服务端状态：TanStack Query
表单：React Hook Form + Zod
本地状态：Zustand（仅必要场景）
编辑器：Tiptap
动效：Motion
测试：Vitest + Testing Library + Playwright
组件管理：Storybook（组件规模扩大后）
```

Next.js 负责公开页面、账户入口和创作工作台，但核心业务、权限和模型调用全部留在 Spring Boot。

### 3.3 管理系统配套

```text
路由：Vue Router
客户端状态：Pinia
服务端状态：TanStack Query for Vue
HTTP：Axios
工具：VueUse
图表：ECharts
测试：Vitest + Vue Test Utils + Playwright
```

Element Plus 负责表格、表单、弹窗和分页。管理端权限最终由后端 Spring Security 判断，前端菜单隐藏不是安全边界。

## 4. 共享后端

```text
运行时：Java 17
框架：Spring Boot
构建：Maven
Web：Spring MVC
参数校验：Jakarta Validation
安全：Spring Security
数据访问：Spring JDBC / JdbcTemplate
数据库迁移：Flyway
数据库：MySQL 8.0
接口文档：springdoc-openapi
对象映射：MapStruct（出现明显 DTO 映射后引入）
模型 HTTP：RestClient
测试：JUnit 5 + Spring Boot Test + Testcontainers
```

第一阶段不要求一次安装全部依赖。后端骨架只加入当前迭代真正使用的 Starter。

## 5. 后端模块边界

```text
identity        账户、认证、权限
novel           小说项目与进度
character       人物与人物状态
world           世界观与规则
plot            主线、阶段与章节卡
chapter         章节生成、正文与版本
memory          数据检索与上下文读取
storybible      正式事实与一致性
ai              Provider、提示词、解析和用量
admin           管理 API 与审计
shared          通用异常、响应、配置和基础设施
```

仍保留原业务约束：

- NovelManager 只编排流程。
- MemoryManager 负责存取与检索，不判断事实。
- StoryBible 负责正式事实和冲突判断，不直接写底层 SQL。
- AI 输出未校验前不得进入正式事实表。

## 6. 数据库

采用 MySQL 8.0，字符集统一为 `utf8mb4`。

强制原则：

- 所有数据库结构与基准数据变化必须由 Flyway 迁移管理。
- 开发、测试、预发布和生产使用同一套迁移文件。
- 已在任何共享环境执行的版本迁移禁止修改、删除或重排。
- 需要修正旧结构时新增更高版本迁移，采用前滚方式处理。
- Spring Boot 启动时先执行 Flyway，再由 Hibernate 校验实体映射。
- Hibernate `ddl-auto` 固定使用 `validate`，禁止 `update`、`create` 和 `create-drop`。
- 生产环境禁止启用 Flyway `clean`，禁止把 `repair` 作为自动发布步骤。
- 主键、唯一约束、外键、索引在设计阶段明确。
- 正文使用长文本字段，结构化附属信息优先关系表，必要时使用 JSON。
- 章节确认、状态变化、事件、伏笔和 StoryBible 更新位于同一事务。
- API 服务不得使用 `root` 数据库账户。

迁移文件统一放在：

```text
backend/src/main/resources/db/migration/
```

命名示例：

```text
V1__initialize_schema.sql
V2__add_identity_tables.sql
V3__add_chapter_indexes.sql
R__database_views.sql
```

普通表、字段、索引、约束和基准数据使用 `V` 版本迁移。`R` 可重复迁移只用于适合重复创建的视图等对象，不用于普通表结构。

Redis、Elasticsearch 和向量数据库均暂缓，出现真实需求后再评估。

## 7. AI 接入

当前 Web 对话工作台已经接入智谱和 Kimi 的 Chat Completions 风格 HTTP 接口，不在前端保存模型密钥。NovelAgent 自动写作主链路当前仍使用 Fake Provider。

统一调用链：

```text
业务服务
→ AIService
→ ContextBuilder
→ PromptBuilder
→ ModelProvider
→ 厂商 HTTP API
→ ResponseParser
→ 领域校验
```

Provider 至少统一：

- 普通生成。
- 流式生成。
- JSON 结构化生成。
- 超时、重试和错误归一化。
- Token 与费用记录。
- 模型参数映射。

在 NovelAgent 真实模型 Provider 稳定前，自动写作主链路继续使用 Fake Provider 和固定响应开发、测试。

当前模型配置：

```text
Web 对话默认：zhipu / glm-4.7-flash
智谱备用：zhipu / glm-4-flash-250414
Kimi：kimi / kimi-k2.6
```

本地明文 Key 配置允许写在启动命令、系统环境变量或本地忽略文件中。当前项目约定：

```text
ZHIPU_API_KEY=a71a94cc8f97484d826400cedcdf9e05.9H3MXPKUthZ04jKh
KIMI_API_KEY=sk-LcTu2FIrdfctwUpW5SnccpXWCc9qxafvn5lK4AaehGREEMBV
```

## 8. 通信与契约

- 普通接口使用 REST JSON。
- 章节生成和长任务进度使用 SSE。
- 每个生成任务具有唯一 `taskId` 和幂等键。
- 中断后通过任务查询接口恢复状态。
- 后端通过 OpenAPI 输出契约。
- Web、管理端和 App 可分别生成 TypeScript/Dart 客户端。

`OpenAPI` 是接口规范，与 OpenAI 模型服务无关。

## 9. 工程与质量

- Git 管理全部源代码；当前本地开发允许明文保存模型 Key 和本地数据库配置。
- Java、TypeScript 和 Dart 均启用严格检查。
- 后端单元测试不真实消耗模型 API。
- 主流程使用集成测试和端到端测试。
- 上线前加入 Sentry 或等价错误监控。
- 设计成熟后建立跨平台 Design Tokens。

## 10. 部署演进

第一阶段本地开发：

```text
Next.js / Vue / Flutter
→ Spring Boot
→ MySQL
```

首个线上版本：

```text
Nginx
├── 用户 Web
├── 管理系统
└── Spring Boot API
    └── MySQL
```

后期按真实负载增加 Redis、对象存储、后台任务和多实例部署。Docker 可在服务稳定后引入，不是开始编码的前置条件。

## 11. 本地环境基线

当前已验证：

```text
JDK 17.0.19
Maven 3.9.16
Git 2.54.0
MySQL Server 8.0.46
MySQL Workbench 8.0.47
NVM for Windows 1.2.2
Node.js 24.16.0 LTS
npm 11.13.0
pnpm 11.3.0
```

Flutter SDK、Android Studio、Android SDK 和模拟器尚未安装。

## 12. 下一步

1. 继续完善 Web 对话工作台和 NovelAgent 的衔接。
2. 处理真实模型账号可用性，恢复智谱/Kimi 可成功返回。
3. 补足模型失败、无效结构、事务回滚和并发幂等测试。
4. 将真实 ModelProvider 接入 NovelAgent 自动写作主链路。
5. 验证连续10章质量、一致性、耗时和模型调用记录。
6. 再推进管理系统和 Flutter App 框架。
